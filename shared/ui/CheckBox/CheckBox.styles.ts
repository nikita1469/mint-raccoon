import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  checkBoxWrapper: {
    width: 20,
    height: 20,
  },
  unchecked: {
    width: 20,
    height: 20,
    borderRadius: 4,
    borderWidth: 1,
    opacity: 0.5,
  },
});
