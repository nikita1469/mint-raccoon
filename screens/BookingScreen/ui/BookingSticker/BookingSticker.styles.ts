import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  sticker: {
    position: "absolute",
    bottom: 15,
    left: 15,
    paddingVertical: 5,
    paddingHorizontal: 12,
    borderRadius: 5,
  },
});
