import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  tablesSchemaRow: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "flex-end",
    gap: 5,
    marginBottom: 30,
  },
});
